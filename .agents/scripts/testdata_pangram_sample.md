---
title: Sample draft
---

# Scheduling

We evaluate the scheduler under load and report the median latency across
twenty runs. The variance is small enough that the median is representative.

```python
def schedule(q):
    return sorted(q, key=lambda t: t.deadline)
```

| Run | Latency |
|---|---|
| 1 | 12ms |

Delving into the transformative landscape of distributed scheduling, it becomes
crucially important to leverage robust and seamless orchestration paradigms that
unlock unprecedented synergies across the stack.

- a list item that is not prose
- another one

The second measurement contradicts the first, and we do not yet know why.
